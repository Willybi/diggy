.venv/
__pycache__/
*.pyc
.env
node_modules/
server/frontend/dist/
.claude/*
!.claude/commands/

_design/
out/

docs/prompts/

# Coverage artifacts
.coverage
.coverage.*

# TIDAL OAuth tokens (dev fallback — never commit)
.tidal_tokens.json

# Local tool data artifacts (C9 hybrid eval: .npz > GitHub's 100 MB limit — never commit)
worker/embedding_backfill/data_hybrid/
