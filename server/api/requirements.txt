fastapi==0.141.1
# starlette is fastapi's ASGI core (transitive) — pinned explicitly so pip-audit
# resolves the coupled fastapi/starlette pair deterministically. Bump the two together.
starlette==1.6.0
# jinja2: since starlette 1.x, `starlette.templating` raises ImportError AT IMPORT
# when jinja2 is absent, and sentry-sdk's StarletteIntegration imports it
# unconditionally at init — so it is now a hard runtime dep even though Diggy uses
# no server-side templates. (Broke prod on the AV2 starlette 0.38→1.6 bump.)
jinja2==3.1.6
uvicorn[standard]==0.32.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
celery[redis]==5.4.0
redis==5.2.0
pydantic[email]==2.10.0
python-dotenv==1.2.2
boto3==1.35.0
requests==2.34.2
httpx==0.27.0
curl_cffi==0.16.0
defusedxml==0.7.1
alembic==1.14.0
psycopg2-binary==2.9.10
# python-jose 3.5.0 (not 3.4.0): the 3.4.x line caps pyasn1<0.5.0 (=0.4.8, which
# carries 4 fixable pyasn1 DoS advisories); 3.5.0 relaxes the cap → pyasn1 0.6.4.
# Required for a green *blocking* pip-audit. HS256-only usage; the transitive
# ecdsa Minerva advisory (no upstream fix) is the sole --ignore-vuln in the gate.
python-jose[cryptography]==3.5.0
google-auth>=2.0
python-multipart==0.0.32
tidalapi==0.8.11
spotifyscraper==3.7.0
sentry-sdk[fastapi,celery]==2.29.1
python-json-logger==3.3.0
# E2.c — estimated-BPM analysis of Deezer previews (worker only). Pulls a large
# native audio stack (~doubles the image, acté) and needs ffmpeg at runtime for
# MonoLoader (installed in the Dockerfile final stage). Pinned to a dev build
# because the 2.1 line ships only pre-releases on PyPI; this is the newest with a
# cp313 manylinux_x86_64 wheel (matches the py3.13 Linux image — no source build).
essentia==2.1b6.dev1389
